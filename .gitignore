node_modules

# A separate clone of the karls-jewelry-inventory-new-build repo that lives
# inside this folder. Ignored so git doesn't try to record it as a submodule.
karls-jewelry-inventory-new-build/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# SQLite database and its snapshots — the inventory data itself never goes to
# GitHub. It's protected by backups/ and OneDrive sync instead.
*.db
*.db-journal
backups/

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

/../generated/prisma
